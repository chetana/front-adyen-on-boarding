# Configuration pour la production sur Cloud Run
VITE_APP_TITLE=Adyen Onboarding - Production
VITE_APP_ENV=production

# Proxy configuration - in production, proxy runs on same server
# No need to specify VITE_PROXY_URL as it defaults to same origin

# Adyen Configuration (à adapter selon vos besoins)
VITE_ADYEN_ENVIRONMENT=live
VITE_ADYEN_CLIENT_KEY=your-adyen-client-key

# Autres configurations
VITE_ENABLE_ANALYTICS=true
VITE_LOG_LEVEL=error